---
description: Run the spec-driven-eval skill against a PRD/spec implementation.
---

Use the `spec-driven-eval` skill to evaluate the requested implementation.

Arguments: $ARGUMENTS

Load and follow the skill exactly. If any required inputs are missing, ask for the smallest set of missing paths or refs before scoring. Keep the evaluation read-only over the subject under review.
