---
description: Drive roadmap execution or roadmap-management PRs through the product-manager skill.
---

Use the `product-manager` skill to handle this product-manager command.

Arguments: $ARGUMENTS

Load and follow the skill exactly. Treat the arguments as the same command surface documented by the skill: `complete <scope>` executes roadmap stories, while management verbs such as `assign`, `park`, `unpark`, `add-spec`, `new-spec`, `reorder`, `revise`, and `release` reshape the roadmap through planning PRs. Preserve every confirmation, clean-tree, `gh`, branch, push, PR, and human-validation guardrail from the skill. When invoking other skills from OpenCode, use the `skill` tool and OpenCode subagent/task/question tools as the referenced skills require.
