---
description: Generate a self-contained interactive HTML PR review report for the current branch.
---

Use the `pr-review-report` skill to review the current branch and write the HTML report.

Arguments: $ARGUMENTS

Load and follow the skill exactly. Resolve and confirm the base branch before reviewing. If the user supplied a base branch, output path, or other preference in the arguments, apply it unless it conflicts with the skill's safety rules.
