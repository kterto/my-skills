# Progress: FIX-20260819T020345Z-48c5 — Gateable-scope guard and installer rollback fixes

**Plan**: [FIX-20260819T020345Z-48c5-gateable-scope-guard-installer-rollback.md](./FIX-20260819T020345Z-48c5-gateable-scope-guard-installer-rollback.md)
**Status**: QA_READY_WITH_WARNINGS
**Created**: 2026-08-19T02:05:26Z

---

## Log

### 2026-08-19T02:05:26Z | ARCHITECT

Created plan `FIX-20260819T020345Z-48c5`. Type: fix. Tasks: 26 across 4 phases (5 optional).

Source CR: `CR-20260819T015653Z-4511` (cycle 2, REQUEST_CHANGES, 2 must-fix / 6 should-fix).

Both Must Fix items became task pairs (test-first, then implementation):

- **MF-1** → Phase 1 (11 tasks). `assertNonEmptyScope` tests `scope.files.length` while `resolveScope` retains any file under a stack root regardless of `SOURCE_FILE_RE`, so a `src/theme.css`-only scope exits 0 `pass` with `gatesRun: ["G5"]` on all four scope kinds. Fix shape per the CR: resolve an `isSource` predicate from the adapter registry and pass it into the guard. SF-3 and SF-4 are folded into the same test file rather than added as parallel cases, per the CR's instruction.
- **MF-2** → Phase 2 (7 tasks). `install.sh`'s `rollback` `continue`s past any skill with no `.old-$name`, so fresh installs are never unwound while `cleanup` claims the destination was restored. Regression test targets the **fresh-install** path (`MV_FAIL_AT=4`, empty destination) plus the plain-retry recovery — the existing cases at `tests/install.sh:96-113` only exercise the overwrite path.

Should Fix items are optional (`(optional)`) with one deliberate exception: **task 3.2 (SF-1b) is required**. The reviewer overturned the tester's premise on SF-1 — `git show 09fa490:…/node-ts.cjs` shows the old `runG1` loop had no `TS_FILE_RE` filter at all, so the current `!entry && !TS_FILE_RE.test(rel)` *preserves* legacy node-ts behaviour and the "mirror dart" fix would itself be a backward-compat break. The adapter change is therefore **Out of Scope**; only the misleading test (whose `CASES` loop passes `entry: undefined`, the one argument where the adapters agree) is closed now, pinning the divergence rather than repairing it. Recording the divergence in ADR-0015 or a backlog note stays optional (3.1).

Structural constraint carried into the plan: `prime-agent/skills/**` is generated by `scripts/build-prime-agent.mjs` (rmSync + rewrite of `destRoot`), so Phase 1/3's plugins-side JS changes require Phase 4's regeneration and `--check` exit 0. Verified that `prime-agent/install.sh` and `prime-agent/tests/` are **outside** `destRoot` and hand-authored, so Phase 2 edits them directly.

`opencode-port-parity` is untriggered — none of the touched paths has a `.opencode/skills/` override port (only `pr-review-report` and `spec-driven-eval` do).

Verification section emitted: this FIX plan touches production code on both runtime islands (`clean-code-gates` JS suite, `prime-agent` shell suite) plus the generated-tree `--check`.

Status: PLANNED. Ready for coder.

### 2026-08-19T02:08:45Z | CODER

Session started. Plan status → IN_PROGRESS.

### 2026-08-19T02:14:10Z | CODER

Completed task: "1.1 Extend `__tests__/fixtures/empty-scope.cjs` with a non-source-under-`src/` shape"
Added `src/theme.css` + `src/styles/tokens.css` to `projectWithDocs`, factored the git setup into
`oneFileCommitRepo(prefix, rel, content)`, and exported `cssOnlyCommitRepo`, `UNGATEABLE_FILES_SCOPE`
(`files:src/theme.css`) and `UNGATEABLE_MODULE_SCOPE` (`module:src/styles`). The existing docs-only
exports are unchanged and still cited. Red observed immediately: the old "an empty project" test
flipped to failing because `src` is no longer emptied by removing `a.ts` — that is task 1.3's red.
Plan tasks remaining: 25 unchecked

### 2026-08-19T02:14:40Z | CODER

Completed task: "1.2 Write failing tests in `__tests__/run.test.cjs` proving a `src/theme.css`-only scope throws for each of the four kinds"
Four tests added asserting `/zero gateable files \(<kind>\)/`. All four red before the fix
(`files:`, `module:`, `project` returned `status: "pass"` exit 0 with `gatesRun: ["G5"]`; `diff:` likewise).
Plan tasks remaining: 24 unchecked

### 2026-08-19T02:14:55Z | CODER

Completed task: "1.3 Fold SF-3 into 1.2"
The project case is now one test, not two: the old `an empty project` test was folded into the
four-kind group rather than left beside a duplicate. It keeps the `fs.rmSync(src/a.ts)` (a.ts must go
or the scope IS gateable) but now asserts `fs.readdirSync(src).length` is non-zero first, so the test
proves an empty *gateable set* over a populated directory rather than an empty directory. The CSS the
directory retains comes from the fixture (task 1.1), which is where the shape belongs.
Plan tasks remaining: 23 unchecked

### 2026-08-19T02:15:05Z | CODER

Completed task: "1.4 Tighten SF-4"
`assert.throws(…, /module/)` → `/zero gateable files \(module\)/`, with the reason recorded above the
test: `/module/` is also satisfied by Node's `Cannot find module` / `MODULE_NOT_FOUND`.
Plan tasks remaining: 22 unchecked

### 2026-08-19T02:15:20Z | CODER

Completed task: "1.5 Write a failing CLI test in `__tests__/cli-e2e.test.cjs`"
`--scope diff:HEAD~1 --out -` over `cssOnlyCommitRepo()`: asserts `status === 3`, no `"status": "pass"`
in stdout, `/zero gateable files/i` and `HEAD~1` in stderr. Red before the fix (exit 0, pass).
Plan tasks remaining: 21 unchecked

### 2026-08-19T02:15:35Z | CODER

Completed task: "1.6 Write failing unit tests for the predicate itself"
Four tests: the guard passes on a mixed scope with one source file; throws on an all-non-source scope;
`sourcePredicate(cfg)` answers `true` for `src/a.ts` and `false` for `src/theme.css` via the resolved
stack's adapter; and AC 3's fallback — a `registerAdapter('bare-stack', …)` exporting no
`SOURCE_FILE_RE` leaves its files gateable. Red before the fix (the two symbols were not exported).
Plan tasks remaining: 20 unchecked

### 2026-08-19T02:16:10Z | CODER

Completed task: "1.7 Implement: add a module-level `sourcePredicate(cfg)` factory in `src/run.cjs`"
Resolves each file's stack with `fileStack(rel, cfg)`, reads that adapter's `SOURCE_FILE_RE` from the
registry at call time, and mirrors `runG5`'s `sourceRe ? … : …` ternary so a missing regex means
gateable. One decision point — `run()`'s complexity is unchanged (AC 11).
Plan tasks remaining: 19 unchecked

### 2026-08-19T02:16:25Z | CODER

Completed task: "1.8 Implement: change `assertNonEmptyScope(scope)` → `assertNonEmptyScope(scope, isSource)`"
Body is now `if (scope.files.some(isSource)) return;`. Called from `run()` as
`assertNonEmptyScope(scope, sourcePredicate(cfg))` at the same call site, after `resolveScope` and
before `resolveGatePlan`. Message text unchanged; `resolveGatePlan`'s `if (!plan.length) return plan;`
untouched (AC 2). Both symbols exported for the unit tests. Green: 205/205.
Plan tasks remaining: 18 unchecked

### 2026-08-19T02:16:35Z | CODER

Completed task: "1.9 Update the doc comment above `assertNonEmptyScope`"
Now states the real predicate ("no file any scoped adapter considers source") instead of the
"zero gateable files" wording that described an intent the code did not implement. The new
`sourcePredicate` carries its own comment explaining why a scoped file is not necessarily a gated one.
Plan tasks remaining: 17 unchecked

### 2026-08-19T02:16:50Z | CODER

Completed task: "1.10 Verify AC 4 by running the full suite"
`node --test` → 205 tests, 205 pass, 0 fail. Was 197/197 before this phase; +8 net (9 new cases, and
the `an empty project` test folded into the four-kind group rather than duplicated). No previously
green test flipped — `exit-codes.test.cjs`, `gate-selection.test.cjs`, `scope-*.test.cjs` and
`run.test.cjs`'s "a non-empty scope still returns a verdict" all still pass.
Plan tasks remaining: 16 unchecked

### 2026-08-19T02:17:30Z | CODER

Completed task: "1.11 Re-verify AC 12"
`SKILL.md:33` and `README.md`'s exit-code table row 3 are true as written — unchanged. One sentence in
README's "Behaviour change — an empty scope now fails" was **false** as written and was corrected: it
claimed the old behaviour was `status: "pass"` with `gatesRun: []`, but MF-1's shape (files resolve
under a stack root, every gate filters them out) exited 0 with a *named* `gatesRun: ["G5"]`. The
sentence now names both shapes. Exit code and message text did not move, so nothing else changed.
Plan tasks remaining: 15 unchecked

### 2026-08-19T02:18:00Z | CODER

**Phase 1 verification** — both gates green.

- `cd plugins/my-skills/skills/clean-code-gates && node --test` → exit 0, 205 tests (≥ 197 + added cases).
- Manual reproduction of the CR's table in a throwaway repo, `src/theme.css` as the sole scoped file:

  | scope | exit | `"status": "pass"` in stdout |
  |---|---|---|
  | `diff:HEAD~2` | 3 | no |
  | `files:src/theme.css` | 3 | no |
  | `module:src/styles` | 3 | no |
  | `project` | 3 | no |

`node scripts/build-prime-agent.mjs --check` is deliberately NOT run at this phase: `prime-agent/skills/`
is a generated copy of the plugins-side skill and is regenerated wholesale in Phase 4, so `--check`
is expected red between Phase 1 and Phase 4. The plan's Phase 1 verification block omits it for that
reason (Overview: "Phase 4's regeneration is mandatory for Phase 1 and 3").

### 2026-08-19T02:21:00Z | CODER

Completed task: "2.1 Write the failing regression case in `prime-agent/tests/install.sh`"
Fresh destination (`$tmp/fresh`, no prior install), existing `mv` shim at `MV_FAIL_AT=4`. Asserts a
non-zero exit, `find "$dest/.prime/agent/skills" -name SKILL.md | wc -l` = 0, and no `.*` staging
leftovers at depth 1. **Red against the pre-fix installer**, with the CR's exact number:
`a failed fresh install left 3 skills behind`.
Plan tasks remaining: 14 unchecked

### 2026-08-19T02:21:20Z | CODER

Completed task: "2.2 Extend 2.1 with AC 6"
A plain retry with no `--force` against the same destination now installs all 11 skills. Red before
the fix (`Refusing to install: existing skill: … (use --force)`), because the 3 survivors collided.
Plan tasks remaining: 13 unchecked

### 2026-08-19T02:21:35Z | CODER

Completed task: "2.3 Extend 2.1 with SF-5's stderr assertion"
The failed run's stderr is captured to a file rather than discarded, and pinned with `grep -q` against
`Install failed — the destination was restored to its previous state.` — the claim is now true.
Plan tasks remaining: 12 unchecked

### 2026-08-19T02:22:10Z | CODER

Completed task: "2.4 Implement the `rollback` fix in `prime-agent/install.sh`"
`[[ -e "$staging/.old-$name" ]] || continue` + `rm -rf` + `mv` became an unconditional
`rm -rf "$destination/$name"` followed by a guarded restore. Written as a full `if` rather than
`[[ … ]] && mv …`: under `set -e` a false test as the loop body's last command makes `rollback` return
1, and `rollback` is called from the EXIT trap, which would then skip the message, the staging cleanup
and `exit "$status"`. The `committed+=` push was NOT reordered — it already sits between the
move-aside `mv` and the commit `mv`, which is exactly what makes the unconditional `rm -rf` correct
for both the "commit mv failed" and "commit mv succeeded" states.
Plan tasks remaining: 11 unchecked

### 2026-08-19T02:22:25Z | CODER

Completed task: "2.5 Update the comment block above `committed`/`rollback`"
Rewritten to say that unwinding a committed name means restoring whatever stood there before, which is
two different things — move `.old-$name` back, or remove outright — and that rolling back only the
first kind is what let a failed fresh install leave a partial tree under a message claiming otherwise.
Plan tasks remaining: 10 unchecked

### 2026-08-19T02:22:40Z | CODER

Completed task: "2.6 Confirm the pre-existing overwrite cases still pass unchanged"
`fail_at` 5 and 6 both still assert 11 of 11 skills and 11 of 11 `PREEXISTING` markers, unmodified.
Traced by hand as well: at call 5 the failure is skill 3's move-aside, so `committed` holds skills 1-2
and skill 3 is still live; at call 6 it is skill 3's commit `mv`, so `committed` holds 1-3 and skill
3's `.old-` copy is restored by the guarded branch. The restore-live-skills path is not weakened.
Plan tasks remaining: 9 unchecked

### 2026-08-19T02:22:55Z | CODER

Completed task: "2.7 (optional) SF-5, remaining two"
Implemented rather than skipped — both are one-line assertion tightenings.
- `tests/install.sh` symlink cases now match `*"Refusing to install: destination path component is a
  symlink:"*".prime"*` / `*"/agent"*` instead of the bare path globs, which the usage banner at
  `install.sh:9` (an exit-2 usage error) also satisfied.
- The `--force` symlink case now asserts the attacker-chosen `outside` directory is **empty**, matching
  its siblings — `test -d` alone only proved the directory survived, not that nothing was written into it.
Plan tasks remaining: 8 unchecked

### 2026-08-19T02:23:40Z | CODER

**Phase 2 verification** — both gates green.

- `cd prime-agent && npm test` → exit 0 (`tests/install.sh` + `tests/parity.sh`).
  Note: `tests/parity.sh` was red on first run purely because Phase 1's plugins-side change left the
  generated `prime-agent/skills/` stale (5 stale files, all `clean-code-gates`). Not a defect and not
  routed as a blocker — it is the ordering the plan builds in. `node scripts/build-prime-agent.mjs` was
  run to clear it so this phase's gate could be asserted honestly; Phase 4 regenerates wholesale again
  and re-asserts `--check`.
- The fresh-install case is confirmed red against the pre-fix `rollback`: run before the four-line
  change it failed with `a failed fresh install left 3 skills behind` — the CR's reproduction exactly.

### 2026-08-19T02:28:00Z | CODER

Completed task: "3.1 (optional) SF-1(a): record the node-ts / dart-flutter G1 divergence"
Implemented rather than skipped — task 3.2 pins the divergence in a test, and a test alone does not
explain why it is deliberate. Wrote `docs/adr/0015-g1-coverage-surface-divergence-between-adapters.md`
(next in sequence after 0014). Records the two guards side by side, the history the reviewer
established (`09fa490`: node-ts's pre-change `runG1` had no `TS_FILE_RE` filter at all, dart's filtered
unconditionally — so today's shapes *preserve* each adapter's legacy behaviour), that `SOURCE_FILE_RE`
/G2/G4/G5 all gate on `TS_FILE_RE` which makes G1 the lone exception, and that closing it is a
deliberate narrowing to take on its own merits, **not** a backward-compat repair. No adapter behaviour
was changed under this task. Kept out of `SKILL.md` per PROJECT-CONTEXT's single-source-of-truth rule.
Plan tasks remaining: 7 unchecked

### 2026-08-19T02:29:00Z | CODER

Completed task: "3.2 SF-1(b) — required, not optional"
Added `node-ts scores a non-source file carrying a coverage entry where dart-flutter does not` to
`__tests__/g1-absent-coverage.test.cjs`. The divergence is legible from the test name alone, as asked.
It asserts **both** sides in one test — node-ts scores `src/schema.json` (1 statements finding at
50% < 85%), dart-flutter returns `[]` for `lib/assets.json` with an equivalent entry — so it cannot be
satisfied by parity in either direction, which a one-sided characterization test could have been.
Green on first run: this pins today's behaviour rather than changing it, so there is no red step to
observe; its value is that it would go red the moment either adapter moved.
Plan tasks remaining: 6 unchecked

### 2026-08-19T02:29:30Z | CODER

Completed task: "3.3 (optional) SF-2"
Implemented. Both tests renamed to what they assert (`an empty {files:,module:} scope exits 3 even with
a gate id that would also be rejected`). The comment attributing the pass to `resolveGatePlan`'s early
return — now unreachable for exactly these inputs, since the empty-scope guard precedes gate selection
— was replaced with the real mechanism: the ordering is the thing under test, and the deeper cause is
what gets reported. `doesNotMatch(r.stdout, /"status": "pass"/)` was near-vacuous on a path that writes
no stdout at all; both now assert `match(r.stderr, /zero gateable files/i)`.
Plan tasks remaining: 5 unchecked

### 2026-08-19T02:29:50Z | CODER

Completed task: "3.4 (optional) SF-6"
Implemented. `oneFileCommitRepo` in `__tests__/fixtures/empty-scope.cjs` now spawns git with
`GIT_CONFIG_GLOBAL=/dev/null` and `git init -q --template=`, so a developer's global `core.hooksPath`
or `init.templateDir` cannot run their hooks inside the fixture repos and flake the tests that now
carry MF-1's closure. Scope note: this covers the fixture's own git calls. `realGitDiff`'s runtime
calls are untouched and do not need it — `diff`, `ls-files`, `update-index` and `rev-parse` run no hooks.
Plan tasks remaining: 4 unchecked

### 2026-08-19T02:30:20Z | CODER

**Phase 3 verification** — both gates green.

- `cd plugins/my-skills/skills/clean-code-gates && node --test` → exit 0, 206 tests, 206 pass.
- 3.3 landed, so the follow-up check was run: both renamed tests pass at exit 3 asserting on stderr
  (`ok 119`, `ok 120`), and the controls survive — `a non-empty scope with an unknown gate id still
  names the gate id` (`ok 121`) and `a non-empty scope with a valid selection still exits 0` (`ok 122`).

### 2026-08-19T02:32:00Z | CODER

Completed task: "4.1 Run `node scripts/build-prime-agent.mjs` to regenerate `prime-agent/skills/`"
Regenerated wholesale: `wrote prime-agent/skills with 11 skills (154 files)`. Picks up Phase 1's
`src/run.cjs`, `README.md`, fixture and test changes and Phase 3's `g1-absent-coverage.test.cjs` /
`gate-selection.test.cjs` changes.
Plan tasks remaining: 3 unchecked

### 2026-08-19T02:32:20Z | CODER

Completed task: "4.2 Assert `node scripts/build-prime-agent.mjs --check` exits 0"
Exit 0 — `prime-agent/skills is up to date (11 skills, 154 files)`.
Plan tasks remaining: 2 unchecked

### 2026-08-19T02:32:45Z | CODER

Completed task: "4.3 Confirm no file under `prime-agent/skills/**` was hand-edited"
Two independent confirmations:
- `--check` exit 0 is itself the structural proof: it byte-compares the tree against what the builder
  produces from `plugins/my-skills/skills/` plus `prime-agent/overlays/*.json`, so a surviving
  hand-edit would have to coincide exactly with build output.
- Direct file comparison of all seven files this session touched in `clean-code-gates`
  (`src/run.cjs`, `README.md`, `__tests__/run.test.cjs`, `cli-e2e.test.cjs`,
  `fixtures/empty-scope.cjs`, `g1-absent-coverage.test.cjs`, `gate-selection.test.cjs`): six are
  byte-identical between the plugins source and the generated copy; `README.md` differs only by the
  builder's documented `~/.claude/skills/…` → `<skill-dir>` path rewrite plus the `<skill-dir>` gloss
  it inserts — build output, not an edit.
No write in this session targeted a path under `prime-agent/skills/`. The two `prime-agent/` files that
WERE edited directly — `install.sh` and `tests/install.sh` — sit outside `destRoot` and are
hand-authored, as the plan's Technical Notes establish.
Plan tasks remaining: 1 unchecked

### 2026-08-19T02:33:00Z | CODER

Completed task: "4.4 Run the full test suite of both islands and confirm green"
`clean-code-gates` `node --test` → exit 0, 206/206. `prime-agent` `npm test` → exit 0.
Plan tasks remaining: 0 unchecked

### 2026-08-19T02:33:05Z | CODER

**Phase 4 verification** — all three gates green.

- `node scripts/build-prime-agent.mjs --check` → exit 0.
- `cd plugins/my-skills/skills/clean-code-gates && node --test` → exit 0, 206 tests, 206 pass.
- `cd prime-agent && npm test` → exit 0 (`tests/install.sh` + `tests/parity.sh`).

### 2026-08-19T02:33:10Z | CODER

All tasks complete. Plan status → DONE.
Total tasks completed this session: 26 (all 4 phases, including all 5 tasks marked optional)

**Acceptance criteria.**

| AC | Evidence |
|---|---|
| 1 | All four scope kinds over a `src/theme.css`-only scope exit 3, no `"status": "pass"` — reproduced manually in a throwaway repo AND pinned by four `run.test.cjs` cases plus a `cli-e2e` case. |
| 2 | Guard placement, call order and message text unchanged; `resolveGatePlan`'s `if (!plan.length) return plan;` untouched. |
| 3 | `sourcePredicate(cfg)` is resolved in `run()` and passed in; the missing-`SOURCE_FILE_RE` fallback mirrors `runG5`'s ternary and is pinned by a `registerAdapter('bare-stack', …)` test. |
| 4 | 206/206 green; no previously-passing test flipped. |
| 5 | Fresh-install rollback leaves 0 `SKILL.md` and no `.*` leftovers after `MV_FAIL_AT=4`. |
| 6 | Plain retry with no `--force` installs all 11. |
| 7 | Case added to `prime-agent/tests/install.sh`, confirmed red pre-fix (`left 3 skills behind`), and pins the stderr claim. |
| 8 | `g1-absent-coverage.test.cjs` gains a two-sided divergence test; ADR-0015 records why it is pinned, not repaired. |
| 9 | Regenerated wholesale; `--check` exit 0; no hand-edit under `prime-agent/skills/` (see 4.3). |
| 10 | `node --test` exit 0 at 206 ≥ 197; `npm test` exit 0. |
| 11 | `sourcePredicate` is a module-level factory with one ternary; `assertNonEmptyScope` has one `some` call; `run()`'s decision count is unchanged. |
| 12 | `SKILL.md:33` and README's exit-code table verified true and unchanged. One README sentence in "Behaviour change — an empty scope now fails" was false as written (it claimed the old behaviour was `gatesRun: []`, but MF-1's shape exited 0 with a *named* `gatesRun`) and was corrected to name both shapes. |

**Invariants.** `opencode-port-parity` is untriggered — none of the touched paths
(`plugins/my-skills/skills/clean-code-gates/`, `prime-agent/install.sh`, `prime-agent/tests/`,
`docs/adr/`) has a `.opencode/skills/` override port; only `pr-review-report` and `spec-driven-eval`
do. No mirroring was required and none was silently skipped. Backward compatibility held: the only
deliberate behaviour change is the empty-scope strictness increase README already documents, and AC 3's
adapter fallback protects third-party registered adapters. Nothing was committed or pushed.

**Note for the reviewer.** The worktree also carries uncommitted work from the parent
`FEAT-20260819T001630Z-be84` / `FIX-20260819T012309Z-b208` plans (this branch is mid-pipeline). This
session's diff is confined to: `plugins/my-skills/skills/clean-code-gates/{README.md,src/run.cjs,
__tests__/run.test.cjs,__tests__/cli-e2e.test.cjs,__tests__/fixtures/empty-scope.cjs,
__tests__/g1-absent-coverage.test.cjs,__tests__/gate-selection.test.cjs}`,
`prime-agent/{install.sh,tests/install.sh}`, `docs/adr/0015-*.md`, and the regenerated
`prime-agent/skills/clean-code-gates/` mirror of those plugins-side files.

---

## Handoff

| From      | To        | Condition                  | Action                                                        |
| --------- | --------- | -------------------------- | ------------------------------------------------------------- |
| ARCHITECT | CODER     | Plan created               | `invoke /coder with plan ID FIX-20260819T020345Z-48c5`        |
| CODER     | REVIEWER  | All tasks [x], status DONE | `invoke /reviewer with plan ID FIX-20260819T020345Z-48c5`     |
| REVIEWER  | ARCHITECT | REQUEST_CHANGES            | `invoke /architect with the new CR file path`                 |
| REVIEWER  | QA        | APPROVED                   | `invoke /qa with plan ID FIX-20260819T020345Z-48c5`           |
| QA        | ARCHITECT | BLOCKED                    | `invoke /architect with the QA report file path`              |

### 2026-08-19T02:28:07Z | TESTER

Test suite complete.
Report: plans/test/TEST-20260819T022100Z-2621-gateable-scope-guard-installer-rollback.md
Status: PASS
Coverage: 86.77% → 86.77%
All e2e flows green. Coverage floor met.

Verified independently of the coder's report — MF-1 is the finding this tester raised as D1 in
`TEST-20260819T014354Z-d58f`, so closure was re-derived rather than accepted.

- **MF-1** re-probed on a purpose-built fixture across 6 non-source extensions x 4 scope kinds: all
  exit 3, none emits `"status": "pass"`, message names the kind and base ref. No legitimate run newly
  exits 3 — mixed scopes (`files:`, `module:`, `diff:`) all return their verdict, and `--scaffold`
  exits 0 on both source-bearing and source-free repos (it short-circuits before `run()`).
- **Escape hatch bounded.** Neither real adapter can reach `sourceRe ? … : true` — both export a
  `SOURCE_FILE_RE`. The adjacent case AC 3 does not name (a user config adding a stack key with no
  registered adapter, permitted by `deepMerge` in `config.cjs:47`) also takes the fallback but does
  **not** false-pass: it exits 3 at gate selection with `gates: {}`, and with `gates: {G5}` a gate
  genuinely scans the file. Recorded so the reviewer need not re-derive it.
- **MF-2** re-probed at 5 failure points (`MV_FAIL_AT` 1, 2, 4, 7, 11) against an empty destination.
  All leave the destination with **zero depth-1 entries of any kind** — stronger than the suite's own
  assertion — print the rollback message, and accept a plain non-`--force` retry installing 11/11.
- **Mutation probe: 5 mutants injected, 5 killed.** Guard reverted to `scope.files.length` → 6 red;
  node-ts G1 narrowed to `TS_FILE_RE` → task 3.2's case red; dart G1 unfiltered → task 3.2's case red
  (so the divergence test is genuinely two-sided, as the plan required); `sourcePredicate` fallback
  flipped to `false` → 5 red; `install.sh` guarded restore removed → red.
- **MF-2 regression test confirmed red pre-fix independently**: reverting only the four-line `rollback`
  body yields `a failed fresh install left 3 skills behind` — the CR's exact number. File restored
  byte-for-byte (md5 verified).
- **AC 12 README sentence** re-derived from a pre-feature baseline binary rather than read: both halves
  are factually true (`gatesRun: []` when nothing resolves; `gatesRun: ["G5"]` for the MF-1 shape). The
  edit was warranted — the old sentence understated the harm. Flagged as a deviation for the reviewer
  to ratify, since AC 12's literal escape clause is narrower than the ground taken.
- Generated tree in sync (`--check` exit 0, 154 files) and passes its own 206/206. No hand-edit under
  `prime-agent/skills/**`; worktree restored to the coder's exact state after every probe.

Weak tests noted (none blocking): `run.test.cjs` mutates the shared `ADAPTERS` registry with no
teardown; `cli-e2e.test.cjs:123-124` carries a redundant assertion pair; the fresh-install case pins a
single `MV_FAIL_AT` where the overwrite cases loop over two. Residual observation: the README's headline
example ("docs-only pull request") is narrower than the actual strictness increase, which also newly
fails `.js/.jsx/.json/.css/.scss/.svg`-only diffs under a TypeScript stack root.

### 2026-08-19T02:30:32Z | REVIEWER

Code review complete.
CR: plans/code-review/CR-20260819T023032Z-d934-gateable-scope-guard-installer-rollback.md
Status: APPROVED
Must Fix: 0 | Should Fix: 5
Ready for QA — invoke /qa with plan ID FIX-20260819T020345Z-48c5.

Both cycle-2 Must Fix findings independently reproduced closed on inputs no prior artifact used:
MF-1 on `.graphql`/`.snap`-only scopes across all four scope kinds (4/4 exit 3, no `status: "pass"`,
mixed and source-bearing scopes still return their verdict); MF-2 at `MV_FAIL_AT=3` and `9` on a
genuinely empty destination (zero depth-1 entries, rollback message present, plain non-`--force`
retry installs 11/11). All 12 ACs met. 206/206 both trees, `npm test` and `--check` exit 0, 15
touched files byte-identical source↔generated.

Rulings on the escalated items:
1. AC 12 README edit — RATIFIED. Both halves of the new sentence are corroborated by this reviewer's
   own cycle-1 and cycle-2 reproductions, independent of the tester's baseline-binary re-derivation.
   AC 12's purpose is that the prose stay true; PROJECT-CONTEXT → Test tooling makes "backward-compat
   claims hold in prose" a structural-review criterion, and the sentence is such a claim. No plan
   amendment needed — the CR is the ratification record.
2. README headline breadth — Should Fix (SF-5), not a blocker. The explanatory paragraph states the
   general rule; only the illustration is gentler. Pure-JS projects confirmed unaffected via
   `detect.cjs` (requires both `package.json` and `tsconfig.json`).
3. Three weak-test notes — all Should Fix (SF-2 registry teardown, SF-3 redundant assertion,
   SF-4 single `MV_FAIL_AT`). None is a false-green risk; none touches a Must Fix closure.
4. Unregistered-stack-key probe — no action. Correct as designed: `sourcePredicate` mirrors `runG5`'s
   ternary, so the predicate cannot claim gateability the gate then declines to deliver.

One new non-blocking finding of the reviewer's own (SF-1): a failed FRESH install leaves the empty
`.prime/agent/skills` chain it created while stderr says the destination "was restored to its previous
state." Pre-existing (`mkdir -p` predates this cycle), breaches no AC, and a plain retry still succeeds
— so not a blocker; either `rmdir` back to the deepest pre-existing ancestor or soften the message.

`opencode-port-parity` untriggered — no path in this cycle's diff sits under `pr-review-report/` or
`spec-driven-eval/`. No scope creep; `prime-agent/skills/**` regenerated, never hand-edited.

### 2026-08-19T02:36:47Z | QA

Precondition check: Plan FIX-20260819T020345Z-48c5 status=DONE, CR=CR-20260819T023032Z-d934 CR status=APPROVED. Proceeding.
Aggregate under evaluation: whole working tree vs base 09fa490 (HEAD == base; all 92 entries uncommitted). Composed by FEAT-20260819T001630Z-be84 + FIX-20260819T012309Z-b208 + FIX-20260819T020345Z-48c5.

### 2026-08-19T02:36:47Z | QA

Ran: cd plugins/my-skills/skills/clean-code-gates && node --test
Result: PASS — Total: 206 | Passed: 206 | Failed: 0 | Skipped: 0

Ran: cd prime-agent/skills/clean-code-gates && node --test (generated tree)
Result: PASS — Total: 206 | Passed: 206 | Failed: 0 | Skipped: 0

Ran: cd prime-agent && npm test (tests/install.sh + tests/parity.sh)
Result: PASS — exit 0; "install ok" + "parity ok"

Ran: node scripts/build-prime-agent.mjs --check
Result: PASS — "prime-agent/skills is up to date (11 skills, 154 files)", exit 0

Ran: lint / format / build
Result: N/A — none configured in-repo (PROJECT-CONTEXT -> Commands). 0 errors.

### 2026-08-19T02:36:47Z | QA

Gate G1 (coverage) — node --test --experimental-test-coverage, base 09fa490 vs HEAD
Result: PASS (plan scope) / WARN (aggregate) — run.cjs 100.00/82.61. Aggregate 77.23/79.69 -> 86.77/85.58. 9/11 changed source files pass; node-ts.cjs (58.36/65.45) and dart-flutter.cjs (64.80/82.02) below threshold — inherited, tool-gated behind missingTool() guards, and improved (+15.94 / +1.09 line).

Gate G2 (cyclomatic complexity) — substitute heuristic (no eslint in repo)
Result: PASS — over-threshold functions 21 -> 19 (net -2). Remaining are pre-existing adapter tool-runners.

Gate G4 (naming) — substitute scan for single-letter identifiers
Result: PASS — 0 new violations; pre-existing loop-scoped m/r only.

Gate G5 (no comments) — dogfooded via the repo's own scanNoComments, base vs HEAD
Result: PASS — production source 43 -> 43 (flat). __tests__ 55 -> 130 (excluded from source by SOURCE_FILE_RE). Shell scripts: 0 indented # lines.

Gate G6 (mutation) — 7 mutants across both Must Fix change sites
Result: PASS — 100% (7/7 killed). 2 injected by QA: predicate revert killed by 6 tests; rollback revert killed with "a failed fresh install left 3 skills behind". Both files restored md5-identical.

Gate G7 (dependency structure) — require-graph cycle analysis, 14 modules
Result: PASS — 0 cycles, clean layering (only downward edges into defaults.cjs).

Gate G8 (rework ratio)
Result: PASS (plan) / WARN (union) — plan under QA 0.00; accumulated union across the 3-plan chain 1.33 = HIGH_REWORK (3 CRs, 2 REQUEST_CHANGES, 2 FIX plans).

Note: `node bin/gates.cjs --scope project` on this repo exits 3 ("zero gateable files") because node-ts's SOURCE_FILE_RE is /\.tsx?$/ and all repo JS is .cjs/.mjs — so G2/G4/G6/G7 are N/A by construction, not MISSING_TOOL. Substitute measurements run instead.

### 2026-08-19T02:36:47Z | QA

QA suite complete.
Report: plans/qa/QA-20260819T023647Z-5465-gateable-scope-guard-installer-rollback.md
Status: READY_WITH_WARNINGS
Test failures: 0 | Lint errors: 0 | Type errors: 0
All blocking checks pass. Safe to commit and open PR. Warning: G8 accumulated union 1.33 (HIGH_REWORK) — flag for human root-cause investigation. Working tree verified restored to exact pre-QA state (92 entries) after mutation probes.
